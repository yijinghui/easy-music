<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easy.mapper.RoomMemberMapper">

    <select id="selectByRoomId" resultType="com.easy.pojo.entity.RoomMember">
        SELECT * FROM tb_room_member
        WHERE room_id = #{roomId} AND online_status = 1
        ORDER BY join_time
    </select>

    <select id="selectByRoomIdAndUserId" resultType="com.easy.pojo.entity.RoomMember">
        SELECT * FROM tb_room_member
        WHERE room_id = #{roomId} AND user_id = #{userId}
    </select>

    <select id="countOnlineMembers" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM tb_room_member
        WHERE room_id = #{roomId} AND online_status = 1
    </select>

</mapper>
